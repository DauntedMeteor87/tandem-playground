# ListRow
Tappable row inside a Card. Provide leading media, title, subtitle, trailing control; `last` drops the divider.

```jsx
<Card>
  <ListRow leading={<Avatar name="Maya P"/>} title="Maya P." subtitle="Trip lead" last/>
</Card>
```