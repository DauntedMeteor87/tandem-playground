# PhotoFrame
Painterly image surface. With `src` shows the photo under a warm grade; without, the hatched placeholder. `protect` adds a bottom gradient for overlaid text.

```jsx
<PhotoFrame src={url} height={260} protect overlay={<h2>Sykes Hot Springs</h2>}/>
```